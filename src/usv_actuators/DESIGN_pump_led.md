# 분수 펌프 · RGB LED 자동 제어 설계 의도

> 6CAN 무인수상정(USV) 프로젝트 · **B2 보드(Arduino UNO Q)** 담당분 설계 메모
> 작성일: 2026-08-27 · 담당: 윤준서 · 브랜치: `feat/b2-pump-led`
>
> **설계 원칙: 다른 파트가 만든 코드는 건드리지 않는다.**
> 이 문서의 모든 변경은 `usv_actuators`(B2) 안에서만 일어납니다.
> GCS와 B1은 **한 글자도 바뀌지 않습니다.**

---

## 1. 한 줄 요약

**물이 더러워지면 분수가 저절로 돌고, 물 상태를 LED 색깔로 알려준다. 사람이 버튼을 누르면 사람 말이 우선이다.**

---

## 2. 이게 뭐 하는 건가 (아주 쉬운 버전)

배 한 척이 호수 위에 떠 있다고 생각해 보세요.

1. 배 밑에 **혀**가 달려 있어요. 물을 계속 맛봅니다. → *B1 보드의 수질 센서*
2. 혀가 "이 물 더러워!" 하고 알려줍니다. → *수질 데이터*
3. 배가 그 말을 듣고 **생각하고 바로 움직입니다.** "더럽네. 분수를 틀자." → *B2 보드*
4. **분수가 돌고**, 동시에 **전등이 빨간색**이 됩니다.
5. 물이 깨끗해지면 분수를 끄고, 전등을 초록색으로 바꿉니다.

전등 색깔은 **신호등**이랑 똑같습니다.

| 색 | 뜻 |
|---|---|
| 🟢 초록 | 물이 깨끗해요 |
| 🟡 노랑 | 그럭저럭이에요 |
| 🔴 빨강 | 더러워요 (← 이때 분수가 돕니다) |

그리고 뭍에 있는 사람이 화면에서 버튼을 누르면, 배는 **사람 말을 먼저 듣습니다.**

---

## 3. 전체 파이프라인

배에서 판단을 담당하는 부분은 **귀가 두 개**입니다. 한쪽 귀로는 물의 상태를 듣고, 다른 쪽 귀로는 사람의 명령을 듣습니다. 그리고 둘을 종합해서 하나의 결정을 내립니다.

```
   [물]                                        [뭍에 있는 사람]
     │ ① 물을 잰다                                    │ ② 버튼을 누른다
     ▼                                                ▼
┌─────────────────────┐                    ┌─────────────────────┐
│ B1 보드             │                    │ GCS (라즈베리파이)   │
│  수질 센서          │                    │  gui_main_node      │
│      │              │                    │  (웹 대시보드)       │
│      ▼              │                    └──────────┬──────────┘
│  water_quality_node │                               │
└──────────┬──────────┘                               │
           │                                          │
   /water_quality/data                    /actuator/pump_cmd
   (물이 몇 점인지 숫자)                    /actuator/led_cmd
           │                                          │
    ~~~~~~~│~~~~~~~~~~~~~ Wi-Fi ~~~~~~~~~~~~~~~~~~~~~~│~~~~~~
           │                                          │
           └──────────────┐          ┌────────────────┘
                          ▼          ▼
┌────────────────────────────────────────────────────┐
│ B2 보드 (내 담당)                                   │
│                                                    │
│         actuator_driver_node                       │
│         ┌────────────────────────┐                 │
│         │ 왼쪽 귀: 물 상태        │                 │
│         │ 오른쪽 귀: 사람 명령    │                 │
│         │        ↓               │                 │
│         │ 사람 말이 우선          │  ③ 결정         │
│         └────────┬───────────────┘                 │
│                  │ ④ 칩에게 부탁                    │
│                  ▼                                 │
│         STM32 칩 (sketch.ino)                      │
│                  │ ⑤ 진짜 전기를 흘림               │
│                  ▼                                 │
│         🚿 분수 펌프      💡 RGB LED                │
└────────────────────────────────────────────────────┘
```

### 흐름을 말로 풀면

| 단계 | 무슨 일이 일어나나 |
|---|---|
| ① | B1 보드에 꽂힌 센서가 물의 탁도·pH·산소·온도를 잽니다 (1초마다) |
| ② | 동시에, 뭍에 있는 사람이 화면에서 버튼을 누를 수도 있습니다 |
| ③ | B2가 ①과 ②를 **서로 다른 두 개의 토픽으로** 받아서 최종 결정을 내립니다 |
| ④ | 결정된 명령을 보드 안의 STM32 칩에게 부탁합니다 |
| ⑤ | 칩이 진짜 전기 핀을 켜서 펌프가 돌고 LED에 불이 들어옵니다 |

### 왜 헷갈리지 않는가

사람 명령과 물 상태가 **다른 토픽으로 도착하기 때문**입니다.

| 들어온 토픽 | 해석 |
|---|---|
| `/actuator/pump_cmd`, `/actuator/led_cmd` | 사람이 버튼을 눌렀다 |
| `/water_quality/data` | 물 상태가 갱신됐다 |

같은 토픽이었다면 "이거 사람이 보낸 건가, 내가 보낸 건가?"를 구분할 방법이 없습니다. 다행히 이미 나뉘어 있어서, **아무것도 새로 만들지 않아도 우선순위를 정할 수 있습니다.**

---

## 4. 노드별 정리

### 4-1. `water_quality_node` — 물을 맛보는 혀

| | |
|---|---|
| **어디서 도나** | B1 보드 |
| **누구 파트** | 다른 담당자 (**이미 완성됨**) |
| **받는 것** | 없음 (센서를 직접 읽음) |
| **내보내는 것** | `/water_quality/data` · `std_msgs/msg/String` |
| **주기** | 1초 |
| **내가 바꿀 것** | **없음** |

**하는 일**: B1 보드 핀에 물린 센서 4종(탁도·pH·용존산소·수온)을 읽어서, JSON 한 덩어리로 만들어 뿌립니다.

**보내는 내용**:
```json
{"ms":..., "temp_c":24.31, "ph":7.02, "do_mg_l":8.44,
 "turbidity_voltage_v":3.210, "clarity_pct":72.4, "clarity_level":"clear"}
```

**내가 쓸 값**: `clarity_pct` (0~100, 클수록 깨끗). `clarity_level`이라는 글자 값도 같이 오지만 **숫자 쪽을 씁니다** — 이유는 5-2 참고.

**의미**: 배의 감각기관입니다. 이게 안 돌면 자동 제어는 아무것도 못 합니다.

> **중요**: 이 노드를 구독하는 데는 **B1 담당자의 코드 수정이 전혀 필요 없습니다.** ROS에서 발행자는 누가 듣는지 알 필요도 없고, 듣는 사람이 늘어도 발행자 코드는 그대로입니다. 다만 "B2도 이 토픽을 듣기 시작했다"는 사실은 팀에 공유해야 합니다 (7절 참고).

---

### 4-2. `actuator_driver_node` — 머리와 손을 겸함 ⭐ 내가 고칠 것

| | |
|---|---|
| **어디서 도나** | B2 보드 |
| **누구 파트** | **나** (기존 파일 수정) |
| **받는 것** | `/water_quality/data` · `String` ← **새로 추가**<br>`/actuator/pump_cmd` · `Bool` (기존, GCS에서)<br>`/actuator/led_cmd` · `ColorRGBA` (기존, GCS에서) |
| **내보내는 것** | 없음 (칩에 직접 RPC 호출) |
| **내가 바꿀 것** | 구독 하나 추가 + 판단 로직 추가 |

**하는 일**: 네 가지입니다.

1. **판정** — `clarity_pct` 숫자를 보고 좋음/보통/나쁨 3단계 중 하나로 정합니다
2. **중재** — 사람이 버튼을 눌렀으면 그 말을 우선합니다
3. **실행** — 결정된 값을 STM32 칩에게 넘깁니다
4. **안전** — B1에서 데이터가 안 오면(Wi-Fi 끊김 등) 펌프를 끕니다

**원래 하던 일**: 받은 명령을 칩에 넘기기만 했습니다. 판단은 전혀 안 했습니다.

```python
Bridge.notify('set_pump', True)               # 펌프 켜라
Bridge.notify('set_actuator_led', 255, 0, 0)  # 빨간불 켜라
```

`ColorRGBA`는 0.0~1.0 실수로 오는데, 이 노드가 0~255 정수로 바꿔서 넘깁니다.

**왜 별도 노드를 안 만드나**: 처음에는 판단만 하는 노드를 따로 만들려 했습니다. 그런데 그러면 그 노드가 GCS와 **같은 토픽에 발행**하게 되어, "누가 보낸 명령인지" 구분이 안 됩니다. 그래서 GCS 쪽 토픽 이름을 바꿔달라고 요청해야 했는데 — **남의 파트를 건드리는 일**이라 접었습니다. 판단을 이 노드 안으로 가져오면 두 입력이 다른 토픽으로 들어오므로 그 문제가 통째로 사라집니다.

**코드는 나눕니다**: 노드 하나에 판단과 실행이 섞이면 나중에 읽기 어려워집니다. 판정 규칙만 `water_policy.py`라는 별도 파일로 빼고, 이 노드가 가져다 쓰는 형태로 만듭니다. **프로세스는 하나, 파일은 둘.**

**레포 규칙상 문제 없나**: README가 명시적으로 허용합니다.
> 토픽 인터페이스(이름/타입)만 준수한다면 **내부 클래스 구조, 파일 분할, 알고리즘 구현은 담당자가 자유롭게 재구성**해도 괜찮습니다.

토픽 이름과 타입은 하나도 안 바뀌므로 규칙을 지킵니다.

---

### 4-3. `water_policy.py` — 판정 규칙만 담은 코드 조각 ⭐ 내가 만들 것

| | |
|---|---|
| **어디서 도나** | 노드가 아님. 4-2가 불러 쓰는 **함수 묶음** |
| **누구 파트** | 나 (신규 파일) |
| **하는 일** | 숫자를 받아 "좋음/보통/나쁨"을 돌려줌 |

ROS와 아무 상관 없는 순수 계산 코드입니다. 그래서 보드 없이 노트북에서도 테스트할 수 있고, 임계값을 바꿔도 하드웨어 코드는 안 건드립니다.

**의미**: "언제 켤지 정하는 규칙"을 한 곳에 모아두는 서랍입니다.

---

### 4-4. `sketch.ino` (STM32 칩) — 실제로 전기를 흘리는 곳 ⭐ 내가 만들 것

| | |
|---|---|
| **어디서 도나** | B2 보드의 STM32 칩 (리눅스가 아니라 **칩 위에서** 직접) |
| **누구 파트** | 나 + 추진기 담당자 (**공동**) |
| **받는 것** | `actuator_driver_node`의 RPC 호출 |
| **내보내는 것** | 실제 핀 전압 |

**하는 일**: 함수 세 개를 준비해 두고 부르면 실행합니다.

```cpp
void set_pump(bool on)                     // 릴레이 켜고 끄기      ← 내 몫
void set_actuator_led(int r, int g, int b) // LED 3색 밝기 조절     ← 내 몫
void set_thruster_pwm(int l, int r)        // 추진기                ← 다른 담당자 몫
```

**주의 1 — 파일이 하나입니다**: B2 보드는 칩이 하나라 **스케치도 하나**입니다. 추진기 담당자의 함수도 같은 파일에 들어가야 합니다. 각자 만들면 하나가 다른 하나를 덮어씁니다. **이건 반드시 함께 작업해야 합니다.**

**주의 2 — PWM 핀이 6개뿐입니다**: UNO Q에서 밝기·세기 조절이 가능한 핀은 `D3, D5, D6, D9, D10, D11` 여섯 개뿐입니다.

| 용도 | 필요한 PWM 핀 |
|---|---|
| RGB LED | **3개** (R·G·B) |
| 추진기 2개 | 모터 드라이버 방식에 따라 **2개 또는 4개** |
| 분수 펌프 | **0개** (켜고 끄기만 하므로 아무 핀이나 가능) |

3 + 4 = 7 이면 **핀이 모자랍니다.** 추진기 담당자에게 "모터 드라이버가 PWM 몇 개 쓰냐"를 먼저 물어봐야 합니다.

---

### 4-5. `gui_main_node` — 뭍에 있는 사람의 화면

| | |
|---|---|
| **어디서 도나** | GCS (지상 통제소, 라즈베리파이) |
| **누구 파트** | 다른 담당자 |
| **내보내는 것** | `/actuator/pump_cmd` · `Bool`<br>`/actuator/led_cmd` · `ColorRGBA` |
| **내가 바꿀 것** | **없음** ✅ |

**하는 일**: 웹 대시보드(`http://<GCS IP>:8000`)에 펌프 버튼과 LED 색상 선택 UI를 띄웁니다. 사람이 누르면 명령을 보냅니다.

**이 설계에서 GCS는 아무것도 안 바꿔도 됩니다.** 지금 쓰는 토픽 이름 그대로 두면 됩니다. B2가 그 명령을 "사람이 누른 것"으로 알아서 해석합니다.

---

### 4-6. `thruster_driver_node` — 추진기 (참고)

| | |
|---|---|
| **어디서 도나** | B2 보드 (**같은 보드, 같은 패키지**) |
| **누구 파트** | 추진기 담당자 |
| **내가 바꿀 것** | **없음** |

제 파트와 직접 얽히지는 않지만, **같은 보드의 같은 스케치 파일과 같은 핀을 나눠 쓰기 때문에** 핀 배치를 반드시 협의해야 합니다 (4-4 주의 2).

---

## 5. 규칙 상세

### 5-1. 3단계 판정

| 단계 | 조건 | LED | 펌프 |
|---|---|---|---|
| 좋음 | `clarity_pct ≥ 60` | 🟢 초록 | OFF |
| 보통 | `40 ≤ clarity_pct < 60` | 🟡 노랑 | OFF |
| 나쁨 | `clarity_pct < 40` | 🔴 빨강 | **ON** |

**펌프는 "나쁨"인 동안 계속 돌고, 보통이나 좋음이 되면 멈춥니다.**

**떨림 방지(히스테리시스)**: 숫자가 39.8 ↔ 40.2로 흔들리면 펌프가 1초마다 딸깍거려 릴레이가 망가집니다. 그래서 한 번 어떤 단계에 들어가면 **경계를 3%p 넘겨야** 빠져나오게 합니다. 예를 들어 나쁨에 들어가면 43 이상이 되어야 보통으로 올라갑니다.

### 5-2. 왜 `clarity_level`(글자)이 아니라 `clarity_pct`(숫자)인가

`clarity_level`은 **B1 보드의 칩 코드 안에서** 이미 5단계로 계산됩니다. 이걸 쓰면 "40% 기준이 빡세니 35로 낮추자" 할 때마다 **다른 담당자의 칩 코드를 고치고 다시 업로드**해야 합니다.

숫자를 받아서 내가 판단하면, 실행 인자 한 줄로 바꿀 수 있고 **남의 파트를 건드릴 일이 없습니다.**

### 5-3. 사람이 버튼을 눌렀을 때 (확정)

**규칙 하나로 통일합니다: 수동 조작은 60초 동안 자동을 이긴다. 60초가 지나면 자동이 다시 판단한다.**

| | 수동 우선 시간 | 그 뒤 |
|---|---|---|
| 펌프 | **60초** | 수질에 따라 다시 판단 |
| LED | **60초** | 수질에 따른 색으로 복귀 |

동작 예시:

```
[물 나쁨]  자동이 펌프 ON, LED 빨강
   ↓
사람이 '펌프 OFF' 누름  → 즉시 꺼짐, 60초 타이머 시작
   ↓
60초 경과 → 물이 아직 나쁨 → 자동이 다시 ON
   ↓
사람이 또 OFF 누름 → 또 60초 꺼짐
```

**왜 "단계가 바뀔 때만 개입"이 아닌가**: 그렇게 하면 물이 30분 내내 나빠도 자동이 영영 안 켭니다. 사람이 끄고 잊어버리면 정화 기능이 죽은 채로 배가 떠 있게 됩니다. 60초 후 복귀는 **사람 우선과 안전을 둘 다** 만족합니다.

**끄는 방법**:
- 펌프 — 대시보드의 `펌프 OFF` 버튼 (이미 있음)
- LED — 색상 선택기에서 **검정(#000000)** 선택 → `r=g=b=0` → 소등 (GUI 수정 불필요)

**⚠️ 한계**: 지금 신호로는 **"껐다"와 "끄고 잠가라"를 구분할 수 없습니다.** `/actuator/pump_cmd`는 `true`/`false` 두 값뿐이라, 버튼을 누르는 행위에 "일시적인가 영구적인가"라는 정보가 안 실립니다. 그래서 60초 타임아웃 말고는 방법이 없습니다.

완전 수동(자동 개입 없음)을 원하면 GCS에 토글이 하나 필요합니다 — 7절의 제안 2번 참고. **다만 그게 없어도 지금 설계는 정상 동작합니다.**

### 5-3-1. `auto_mode` 대비 (미리 심어두기)

코드에 `/actuator/auto_mode` (`std_msgs/msg/Bool`) 구독을 **미리 넣어두되, 그 토픽이 안 오면 자동=켬으로 동작**하게 만듭니다.

| 상황 | 동작 |
|---|---|
| 지금 (GCS가 안 보냄) | 위 60초 타임아웃 방식. **아무 의존 없음** |
| 나중에 GCS가 토글 추가 | 체크 해제 시 **완전 수동**. B2 코드는 한 줄도 안 고침 |

ROS는 아무도 발행하지 않는 토픽을 구독해도 에러가 나지 않으므로 안전합니다.

### 5-4. 데이터가 끊기면

B1이 죽거나 Wi-Fi가 끊겨서 **5초 넘게 수질 데이터가 안 오면 펌프를 끕니다.** 켜진 채로 방치되면 배터리만 축납니다.

---

## 6. 아직 안 정해진 것

- [ ] **핀 배치** — 추진기 담당자와 함께 정해야 함. PWM 핀 6개를 나눠 써야 하고, RGB LED가 3개 필요
- [ ] **릴레이 사양** — UNO Q는 3.3V 로직. 5V 트리거 릴레이는 오동작 가능
- [ ] **RGB LED 종류** — 단품인지 스트립인지, 공통 애노드인지 캐소드인지 (배선 반전 여부가 갈림)
- [x] ~~수동 복귀 정책~~ — **확정: 펌프·LED 모두 60초 후 자동 복귀** (5-3)
- [ ] **임계값 실측** — 실제 수조에서 `clarity_pct`가 어느 범위로 나오는지 보고 40/60/60초를 조정

---

## 7. 팀에 전할 내용

### 요구사항: **없음** ✅

이 설계는 다른 파트에 아무 변경도 요구하지 않습니다. GCS도 B1도 지금 코드 그대로입니다.

### 공유사항 1건

> **B2가 `/water_quality/data`를 구독하기 시작합니다.**
> 지금까지 이 토픽은 GCS만 들었습니다. B2가 자동 제어를 위해 함께 듣게 됩니다.
> B1 담당자가 코드를 고칠 필요는 없지만, **토픽 이름이나 JSON 필드명(`clarity_pct`)을 바꾸시면 미리 알려주세요.** 말없이 바뀌면 B2의 자동 제어가 조용히 멈춥니다.

### 제안 2건 (둘 다 강제 아님 · 없어도 동작함)

**제안 1 — 펌프·LED의 현재 상태를 알려주는 토픽**

> 지금은 GCS가 자기가 보낸 명령만 알고 실제 상태는 모릅니다. 자동 제어가 들어가면 화면 표시와 실제가 어긋날 수 있습니다.
> 예: `/actuator/pump_state` (`Bool`), `/actuator/led_state` (`ColorRGBA`)

**제안 2 — 대시보드에 "자동 제어" 체크박스**

> 현재 `/actuator/pump_cmd`는 `true`/`false` 두 값뿐이라, "잠깐 꺼줘"와 "계속 꺼둬"를 구분할 수 없습니다. 그래서 수동 조작이 60초만 유지됩니다.
> 체크박스 하나와 `/actuator/auto_mode` (`std_msgs/msg/Bool`) 발행을 추가해 주시면, 체크 해제 시 완전 수동이 됩니다.
> **B2 쪽은 이미 이 토픽을 구독하도록 만들어 두었으므로, GCS에서 발행만 시작하면 바로 동작합니다.**

둘 다 GCS 쪽 작업이 생기므로 **여유 되실 때 검토** 부탁드립니다. 없어도 지장 없습니다.

---

## 8. 참고 — 현재 진행 상황

- ✅ B2 보드 환경 세팅 완료 (Debian 13 · Docker · ROS 2 Jazzy 이미지 빌드 · 노드 기동 확인)
- ✅ 팀 레포 클론 (`~/ArduinoApps/6can_usv_ws`, 브랜치 `feat/b2-pump-led`)
- ⬜ `water_policy.py` 작성 (판정 규칙)
- ⬜ `actuator_driver_node.py` 수정 (수질 구독 + 중재 로직)
- ⬜ `sketch.ino` 작성 (핀 배치 확정 후, 추진기 담당자와 공동)
- ⬜ 실제 배선 및 동작 검증
