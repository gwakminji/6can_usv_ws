# API 키 등 git에 올리면 안 되는 값들을 여기 적는다.
# 사용법: 이 파일을 같은 폴더에 gcs_secrets.yaml 이름으로 복사한 뒤 값을 채운다.
#   cp gcs_secrets.yaml.example gcs_secrets.yaml
# gcs_secrets.yaml은 .gitignore에 등록되어 있어 커밋되지 않는다.

# Google Maps Static API 키 - 미니맵 위성 사진에 쓰인다. 비워두면 미니맵이 빈 파란
# 박스로 표시된다 (동작에는 지장 없음). 발급: https://console.cloud.google.com/
google_maps_api_key: "AIzaSyAn7HQLTvwf2JIDEOtdJZfxXyEGBeAmTSg"